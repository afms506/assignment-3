<!-- 
    HEADER PARTIAL
    This file contains:
    - HTML document structure
    - Bootstrap + custom CSS links
    - Dark/Light mode logic using localStorage
    - Navigation bar for the entire site
-->

<!DOCTYPE html>
<html>
<head>
    <title>Event Guest List Manager</title>

    <!-- Custom CSS -->
    <link rel="stylesheet" href="/css/style.css">

    <!-- Bootstrap CSS -->
    <link 
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" 
        rel="stylesheet"
    >

    <script>
        /* 
           THEME HANDLING SCRIPT
           - Checks saved theme in localStorage
           - Applies light or dark mode BEFORE rendering page (prevents flicker)
        */

        const savedTheme = localStorage.getItem("theme");

        // Apply saved mode to <html> element
        if (savedTheme === "dark") {
            document.documentElement.classList.add("dark-mode");
        } else {
            document.documentElement.classList.add("light-mode");
        }

        // Toggle between light & dark mode
        function toggleTheme() {
            const html = document.documentElement;
            const btn = document.querySelector(".theme-btn");

            if (html.classList.contains("dark-mode")) {
                html.classList.remove("dark-mode");
                html.classList.add("light-mode");
                localStorage.setItem("theme", "light");
                btn.textContent = "Dark Mode";
            } else {
                html.classList.remove("light-mode");
                html.classList.add("dark-mode");
                localStorage.setItem("theme", "dark");
                btn.textContent = "Light Mode";
            }
        }

        // Update button text after page loads
        window.onload = () => {
            const btn = document.querySelector(".theme-btn");
            btn.textContent = 
                localStorage.getItem("theme") === "dark"
                ? "Light Mode"
                : "Dark Mode";
        };
    </script>
</head>

<body>

<!-- 
    NAVIGATION BAR 
    - Uses Bootstrap responsive navbar
    - Includes brand link
    - Includes Theme Toggle button
-->
<nav class="navbar navbar-expand-lg bg-light shadow-sm py-3 main-nav">
  <div class="container">

    <!-- Brand logo/title -->
    <a class="navbar-brand fw-bold" href="/events">Guest Manager</a>

    <!-- Mobile collapse toggle -->
    <button 
        class="navbar-toggler" 
        type="button" 
        data-bs-toggle="collapse" 
        data-bs-target="#navMenu">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navMenu">
      
      <!-- 
          EMPTY UL 
          This is intentionally left blank to push the theme button to the right.
      -->
      <ul class="navbar-nav ms-auto me-3"></ul>

      <!-- Theme switch button -->
      <button 
          onclick="toggleTheme()" 
          class="btn btn-outline-secondary btn-sm theme-btn">
        Dark Mode
      </button>

    </div>
  </div>
</nav>

<!-- Page wrapper -->
<div class="container py-5 page-container">
